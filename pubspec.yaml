name: pdf_read
description: "rojeto de leitura de arquivos PDF com Flutter"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3
dependencies:
  flutter:
    sdk: flutter
  flutter_pdfview: 1.3.2
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  file_picker: ^8.1.4
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true